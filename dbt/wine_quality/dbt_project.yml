name: "wine_quality"
version: "1.0.0"

config-version: 2

profile: "wine_quality"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  wine_quality:
    staging:
      +materialized: view
    core:
      +materialized: table
